#include <stdio.h>

int convert(int fahr);

int main(void)
{
    int fahr;

    for (fahr = 0; fahr <= 300; fahr += 20) {
        printf("%d\t%d\n", fahr, convert(fahr));
    }

    return 0;
}

int convert(int fahr)
{
    return 5 * (fahr-32) /9;
}
