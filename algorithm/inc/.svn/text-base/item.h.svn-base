/*
 * item.h
 */

#ifndef __ITEM_H
#define __ITEM_H

typedef int Item;
typedef int Key;

#define NULLitem    ((Item) -1)
#define KEY(A)      A
#define LESS(A, B)  ((A) < (B))
#define EQ(A, B)    ((A) == (B))

#define DIGIT(item, k)  ((((unsigned)(item)) >> (8-1-(k))) & 1)

#endif
