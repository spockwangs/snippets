/*
 * swap - Swap 2 elements in an array.
 *
 * Author: wbb
 * Date: 2010-03-07
 */

#include "../inc/algorithm.h"

void swap(int A[], int i, int j)
{
    int tmp = A[i];
    A[i] = A[j];
    A[j] = tmp;
}
